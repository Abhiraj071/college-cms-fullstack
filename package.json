{
  "name": "college-cms-fullstack",
  "version": "1.0.0",
  "description": "Fullstack deployment for College CMS",
  "main": "backend/server.js",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
